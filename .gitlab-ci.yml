image: registry.gitlab.com/csicar/pskt

build:
   stage: build
   script:
      - make
      - make install
      - pwd
      - ls /root/.local/bin
      - cp /root/.local/bin/pskt .
   artifacts:
      paths:
      - pskt

test:
   stage: test
   script:
      - purs
      - make test