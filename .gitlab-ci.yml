image: registry.gitlab.com/csicar/pskt

build:
   stage: build
   script:
      - make
      - make install
      - pwd
      - ls /root/.local/bin
   artifacts:
      paths:
      - /root/.local/bin

test:
   stage: test
   script:
      - make test